# Plan: Update README.md

## Scope
Single file edit. Target: `README.md` in the repo root.

## Task
Add a "Quick Start" section to `README.md` with:
1. Prerequisites (Node 20+, pnpm)
2. Clone + install steps
3. Environment setup (copy .env.example)
4. Run dev server command

## Constraints
- Edit ONLY `README.md` — no other files.
- Keep existing content intact; append the new section after the first heading.
- Use fenced code blocks for shell commands.
- Maximum 30 lines of new content.

## Acceptance
- `README.md` contains a "Quick Start" section with working shell commands.
- No other files modified.
